#include "main.h"

/**
 * main - test for function that prints if a string is positive or negative
 *
 * Return: Always 0
 */
int main(void)
{
	int i;

	i = 0;
	positive_or_negative(i);

	return (0);
}
